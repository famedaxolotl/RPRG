ggplot(mtcars, aes(cyl, mpg)) + geom_col()

mtcars|>
  filter(gear==3)|>
  select(mpg, disp)
  

#use lubridate for the dates

stocks25=read_delim("stocks25.txt", )

stocks25$co_stkdate <- dmy(stocks25$co_stkdate)
filter(stocks==s
       
       
       #monthly returns, yearly returns, 2025 returns, 
       c=0+sqrt(-1)
       class(c)
